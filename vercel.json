{
    "buildCommand": "npm run build",
    "devCommand": "vite --port $PORT",
    "installCommand": "npm install",
    "framework": null,
    "outputDirectory": "dist",
    "rewrites": [
        { "source": "/api/mikrotik/:path*", "destination": "/api/mikrotik/[...path]" }
    ]
}
